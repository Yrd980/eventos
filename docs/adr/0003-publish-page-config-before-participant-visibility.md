# Publish Page Config Before Participant Visibility

Operators edit draft Page Config and Blocks in the CMS, while participants only read the activity-level published configuration. Publishing at the activity level keeps incomplete CMS edits out of live activities, avoids half-published page combinations across Home, Agenda, Expo, and Me, and gives the platform a clear version point for audit, rollback, and frontend cache invalidation.
