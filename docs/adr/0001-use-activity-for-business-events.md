# Use Activity for the Business Entity

The platform uses `Activity` as the canonical business entity for summits, forums, exhibitions, launches, trainings, and roadshows. `Event` is reserved for technical occurrences such as realtime messages, audit records, and integration payloads, because using `Event` for both the product entity and system events would make contracts, APIs, and database names ambiguous.
