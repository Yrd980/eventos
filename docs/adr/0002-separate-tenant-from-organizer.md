# Separate Tenant from Organizer

The platform separates `Tenant` from `Organizer`: tenant is the data isolation and administration boundary, while organizer is the attendee-facing host or brand on an activity. This allows one tenant to operate multiple organizer brands or present joint organizers without weakening tenant-scoped permissions, ownership, and CMS isolation.
